/**
 * Spring configuration classes:
 * – RestClientConfig to expose RestTemplate
 * – SecurityConfig to wire up authentication, filters, and password encoding.
 */
package org.example.oopproject1.config;